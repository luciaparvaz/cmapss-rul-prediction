# Dependencias fijadas del proyecto NASA C-MAPSS FD001.
# Generado a partir del entorno en el que se validó y ejecutó de punta a
# punta cada fase (scripts .py y notebooks .ipynb) — ver README.md.
# Python 3.12.3
#
# Instalación:
#   pip install -r requirements.txt
#
# Por qué versiones fijadas y no rangos: la comparación numérica del
# scaler global (fase0_preprocesado.py) ya mostró un
# InconsistentVersionWarning de scikit-learn al cargar un pickle
# generado con una versión distinta a la instalada — fijar versiones
# exactas es lo mínimo para que "reproducible" signifique algo más que
# "funcionó en mi máquina".

numpy==2.0.2
pandas==2.2.3
scikit-learn==1.6.1
xgboost==2.1.3
lightgbm==4.7.0
shap==0.52.0
numba==0.67.0  # dependencia transitiva de shap; 0.60.0 (default previo) no soporta numpy 2.x
joblib==1.4.2
matplotlib==3.10.0
scipy==1.17.1
seaborn==0.13.2
reportlab==5.0.1

# Notebooks (fase0_preprocesado.ipynb, fase2_feature_engineering.ipynb, ...)
nbformat==5.10.4
nbconvert==7.16.4
ipykernel==6.28.0
jupyter_core==5.7.2
